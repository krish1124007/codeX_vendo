# VendorBridge environment
# Postgres connection (not required while running on the in-memory store)
DATABASE_URL="postgresql://USER:admin@localhost:5432/vendorbridge?schema=public"

# Auth
AUTH_SECRET="change-me-in-production"
